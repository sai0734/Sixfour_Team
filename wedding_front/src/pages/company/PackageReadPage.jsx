import AdminLayout from "../../layouts/AdminLayout";
import PackageReadComponent from "../../components/company/PackageReadComponent";

const PackageReadPage = () => {
  return (
    <AdminLayout>
      <PackageReadComponent />
    </AdminLayout>
  );
};

export default PackageReadPage;
